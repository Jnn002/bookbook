# TODO: evalute if we will ned to use this classes
"""@dataclass
class DomainBookDetail(DomainBook):
    reviews: list[DomainReview]
    tags: list"""

""" @dataclass
class DomainBookCreate:
    title: str
    author: str
    publisher: str
    published_date: str
    page_count: int
    language: str
 """
 
 """ @dataclass
class DomainReviewCreateModel:
    rating: int
    review_text: str


# TODO: Check if we need to add book_uid and user_uid to this model
 """

""" 
@dataclass
class DomainTagCreateModel:
    name: str


@dataclass
class DomainTagAddModel:
    tags: list[DomainTagCreateModel]
 """